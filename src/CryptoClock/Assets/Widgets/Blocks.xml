<widget description="Latest blockchain blocks">
  <binding sizes="*x2,*x3,*x4,*x5">

    <row>
      {{
      cols = Config.Placement.Cols // 4
      for i in 0..cols
      }}
      <column>
        {{ if Config.Placement.Rows > 3 }}
          <image align="center" size="large" spacing="none" name="Assets/Block.png" />
          <text size="small" align="center">{{ Model.LastBlocks[i].Height }}</text>  
        {{ else }}
          <image align="center" size="medium" spacing="none" name="Assets/Block.png" />
          <text size="xsmall" weight="bold" align="center">{{ Model.LastBlocks[i].Height }}</text>
        {{ end }}
        {{ if Config.Placement.Rows > 4 }}
          <line size="tiny" foreground="{{ Config.Background }}" />
        {{ end }}
        {{ if Config.Placement.Rows > 3 }}
          <text size="xsmall" align="center">{{ Model.LastBlocks[i].FormattedSize }}</text>
        {{ end }}
        {{ if Config.Placement.Rows > 2 }}
          <text size="xsmall" align="center">{{ Model.LastBlocks[i].AvgFeeRate }} sat/vB</text>
          {{ if Config.Placement.Rows > 4 }}
            <text size="xsmall" align="center">{{ Model.LastBlocks[i].MinFeeRate }} - {{ Model.LastBlocks[i].MaxFeeRate }} sat/vB</text>
          {{ end }}
          <text size="xsmall" align="center">{{ Model.LastBlocks[i].Transactions }} transactions</text>
        {{ end }}
        <text size="xsmall" align="center">{{ Model.LastBlocks[i].MinutesAgo }} minutes ago</text>
      </column>
      {{ end }}
    </row>
    
  </binding>
</widget>